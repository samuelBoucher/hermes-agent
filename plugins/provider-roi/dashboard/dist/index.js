/* Provider ROI dashboard plugin. Plain IIFE: all runtime dependencies arrive via the SDK. */
(function () {
  "use strict";
  const SDK = window.__HERMES_PLUGIN_SDK__;
  if (!SDK) return;
  const { React } = SDK;
  const h = React.createElement;
  const { Card, CardHeader, CardTitle, CardContent, Badge, Button, Input, Label, Select, SelectOption, Separator } = SDK.components;
  const { useState, useEffect, useCallback, useMemo } = SDK.hooks;
  function tone(action) { return { keep: "success", observe: "secondary", reassign: "warning", downgrade: "warning", cancel: "destructive", exclude: "outline" }[action] || "secondary"; }
  function number(value) { return Number(value || 0).toLocaleString(); }
  function money(value, currency) { return new Intl.NumberFormat(undefined, { style: "currency", currency: currency || "USD" }).format(value || 0); }
  function api(path, options) { return SDK.fetchJSON("/api/plugins/provider-roi" + path, options ? Object.assign({ headers: { "Content-Type": "application/json" } }, options) : undefined); }
  function SortButton(props) { return h("button", { className: "provider-roi-sort", type: "button", onClick: props.onClick, "aria-label": "Sort by " + props.label }, props.label, props.active ? (props.desc ? " ↓" : " ↑") : " ↕"); }
  function useLocalSort(items) {
    const [sort, setSort] = useState({ key: "activity", desc: true });
    const sorted = useMemo(function () { return items.slice().sort(function (a, b) { const left = a[sort.key] || 0; const right = b[sort.key] || 0; return (left > right ? 1 : left < right ? -1 : 0) * (sort.desc ? -1 : 1); }); }, [items, sort]);
    return [sorted, sort, function (key) { setSort(function (old) { return { key: key, desc: old.key === key ? !old.desc : true }; }); }];
  }
  function Summary(props) {
    const stats = props.summary || {};
    return h("div", { className: "provider-roi-summary", "aria-label": "Provider ROI summary" }, [["Activity", number(stats.activity)], ["Global delivered", number(stats.delivered)], ["Global approved", number(stats.approved)], ["Global derived friction", number(stats.friction)]].map(function (item) { return h(Card, { key: item[0] }, h(CardContent, { className: "provider-roi-stat" }, h("span", null, item[0]), h("strong", null, item[1]))); }));
  }
  function ProviderRow(props) {
    const provider = props.provider; const [expanded, setExpanded] = useState(false); const [saving, setSaving] = useState(false); const [notice, setNotice] = useState("");
    function toggle() { setSaving(true); api("/settings", { method: "POST", body: JSON.stringify({ kind: "provider", provider: provider.provider, included: !provider.rule.included }) }).then(function () { setNotice("Saved"); props.onRefresh(); }).catch(function (err) { setNotice("Save failed: " + (err.message || err)); }).finally(function () { setSaving(false); }); }
    const plan = provider.rule.plan || {};
    return h("li", { className: "provider-roi-provider" },
      h("div", { className: "provider-roi-row" }, h("button", { className: "provider-roi-name", type: "button", onClick: function () { setExpanded(!expanded); }, "aria-expanded": expanded }, provider.rule.label || provider.provider), h("span", null, number(provider.activity)), h("span", null, "Provider-attributed only"), h("span", null, provider.coverage), h(Badge, { tone: tone(provider.verdict.action) }, provider.verdict.action), h(Button, { size: "sm", variant: "outline", onClick: toggle, disabled: saving || provider.provider === "nous" || provider.provider === "openrouter" }, saving ? "Saving…" : provider.rule.included ? "Exclude" : "Include")),
      notice ? h("p", { className: "provider-roi-notice", role: "status" }, notice) : null,
      expanded ? h(Card, { className: "provider-roi-detail" }, h(CardContent, null, h("dl", { className: "provider-roi-detail-grid" }, h("div", null, h("dt", null, "Classification"), h("dd", null, provider.rule.classification)), h("div", null, h("dt", null, "Plan CAD"), h("dd", null, money(plan.monthly_cost_cad, "CAD"))), h("div", null, h("dt", null, "Usage cost"), h("dd", null, money(provider.usage_cost_usd))), h("div", null, h("dt", null, "Cost provenance"), h("dd", null, provider.cost_status)), h("div", null, h("dt", null, "Profiles"), h("dd", null, provider.profiles.join(", ") || "None")), h("div", null, h("dt", null, "Verdict"), h("dd", null, provider.verdict.reason)), h("div", null, h("dt", null, "Quota"), h("dd", null, provider.manual_quota ? number(provider.manual_quota.used_percent) + "% manual" : "No manual quota"))), h(Separator, { className: "my-3" }), h("h4", null, "Models and tasks"), h("ul", { className: "provider-roi-models" }, provider.models.map(function (model, index) { return h("li", { key: index }, model.model + " · " + model.task + " · " + number(model.api_calls) + " calls"); })))) : null
    );
  }
  function PlanForm() {
    const [form, setForm] = useState({ provider: "", amount: "", currency: "CAD", cad: "", costStatus: "estimated", renewal: "", status: "active", note: "" }); const [message, setMessage] = useState("");
    function change(key) { return function (event) { const value = event.target.value; setForm(function (old) { const next = Object.assign({}, old); next[key] = value; return next; }); }; }
    function submit(event) { event.preventDefault(); api("/settings", { method: "POST", body: JSON.stringify({ kind: "provider", provider: form.provider, plan: { amount: Number(form.amount), currency: form.currency, monthly_cost_cad: Number(form.cad), cost_status: form.costStatus, renewal_on: form.renewal || null, status: form.status, note: form.note } }) }).then(function () { setMessage("Plan saved locally."); }).catch(function (err) { setMessage("Plan not saved: " + (err.message || err)); }); }
    return h("form", { className: "provider-roi-form", onSubmit: submit }, h("h3", null, "Local plan editor"), h(Label, { htmlFor: "roi-plan-provider" }, "Provider"), h(Input, { id: "roi-plan-provider", value: form.provider, required: true, maxLength: 64, onChange: change("provider") }), h(Label, { htmlFor: "roi-plan-amount" }, "Original amount"), h(Input, { id: "roi-plan-amount", type: "number", min: 0, step: "0.01", required: true, value: form.amount, onChange: change("amount") }), h(Label, { htmlFor: "roi-plan-currency" }, "Original currency"), h(Input, { id: "roi-plan-currency", value: form.currency, required: true, minLength: 3, maxLength: 3, onChange: change("currency") }), h(Label, { htmlFor: "roi-plan-cad" }, "Monthly cost CAD"), h(Input, { id: "roi-plan-cad", type: "number", min: 0, step: "0.01", required: true, value: form.cad, onChange: change("cad") }), h(Label, { htmlFor: "roi-plan-cost-status" }, "Cost confirmation"), h(Select, { id: "roi-plan-cost-status", value: form.costStatus, onChange: change("costStatus") }, h(SelectOption, { value: "estimated" }, "Estimated"), h(SelectOption, { value: "confirmed" }, "Confirmed")), h(Label, { htmlFor: "roi-plan-renewal" }, "Renewal date (optional)"), h(Input, { id: "roi-plan-renewal", type: "date", value: form.renewal, onChange: change("renewal") }), h(Label, { htmlFor: "roi-plan-status" }, "Plan status"), h(Select, { id: "roi-plan-status", value: form.status, onChange: change("status") }, h(SelectOption, { value: "active" }, "Active"), h(SelectOption, { value: "trial" }, "Trial"), h(SelectOption, { value: "cancellation-planned" }, "Cancellation planned")), h(Label, { htmlFor: "roi-plan-note" }, "Note"), h(Input, { id: "roi-plan-note", value: form.note, maxLength: 500, onChange: change("note") }), h(Button, { type: "submit", variant: "outline" }, "Save plan"), message ? h("span", { role: "status" }, message) : null);
  }
  function QuotaForm() { const [provider, setProvider] = useState(""); const [percent, setPercent] = useState(""); const [message, setMessage] = useState(""); function submit(event) { event.preventDefault(); api("/settings", { method: "POST", body: JSON.stringify({ kind: "manual_quota", provider: provider, used_percent: Number(percent) }) }).then(function () { setMessage("Manual quota recorded with timestamp."); setPercent(""); }).catch(function (err) { setMessage("Quota not saved: " + (err.message || err)); }); } return h("form", { className: "provider-roi-form", onSubmit: submit }, h(Label, { htmlFor: "roi-quota-provider" }, "Manual quota provider"), h(Input, { id: "roi-quota-provider", value: provider, required: true, placeholder: "provider id", onChange: function (e) { setProvider(e.target.value); } }), h(Label, { htmlFor: "roi-quota-percent" }, "Used percent"), h(Input, { id: "roi-quota-percent", type: "number", min: 0, max: 100, required: true, value: percent, onChange: function (e) { setPercent(e.target.value); } }), h(Button, { type: "submit", variant: "outline" }, "Record quota"), message ? h("span", { role: "status" }, message) : null); }
  function ExceptionForm() { const [provider, setProvider] = useState(""); const [reason, setReason] = useState(""); const [expires, setExpires] = useState(""); const [message, setMessage] = useState(""); function submit(event) { event.preventDefault(); api("/settings", { method: "POST", body: JSON.stringify({ kind: "exception", provider: provider, reason: reason, expires_on: expires }) }).then(function () { setMessage("Exception saved."); setReason(""); }).catch(function (err) { setMessage("Exception not saved: " + (err.message || err)); }); } return h("form", { className: "provider-roi-form", onSubmit: submit }, h(Label, { htmlFor: "roi-exception-provider" }, "Exception provider"), h(Input, { id: "roi-exception-provider", value: provider, required: true, onChange: function (e) { setProvider(e.target.value); } }), h(Label, { htmlFor: "roi-exception-reason" }, "Reason"), h(Input, { id: "roi-exception-reason", value: reason, required: true, maxLength: 500, onChange: function (e) { setReason(e.target.value); } }), h(Label, { htmlFor: "roi-exception-expires" }, "Expiry date"), h(Input, { id: "roi-exception-expires", type: "date", value: expires, required: true, onChange: function (e) { setExpires(e.target.value); } }), h(Button, { type: "submit", variant: "outline" }, "Save exception"), message ? h("span", { role: "status" }, message) : null); }
  function ProviderRoiPage() {
    const [report, setReport] = useState(null); const [error, setError] = useState(""); const [month, setMonth] = useState(""); const load = useCallback(function () { setError(""); api("/overview" + (month ? "?month=" + encodeURIComponent(month) : "")).then(setReport).catch(function (err) { setError(err.message || "Unable to load Provider ROI."); }); }, [month]); useEffect(function () { load(); }, [load]); const providers = report && Array.isArray(report.providers) ? report.providers : []; const sorted = useLocalSort(providers); const list = sorted[0]; const sort = sorted[1]; const setSort = sorted[2];
    return h("section", { className: "provider-roi", "aria-busy": !report && !error }, h("header", { className: "provider-roi-header" }, h("div", null, h("h1", null, "Provider ROI"), h("p", null, "Provider-first usage with explicit coverage and cancellation safeguards.")), h("div", { className: "provider-roi-controls" }, h(Label, { htmlFor: "roi-month" }, "Reporting month"), h(Input, { id: "roi-month", type: "month", value: month, onChange: function (e) { setMonth(e.target.value); } }), h(Button, { onClick: load, variant: "outline" }, "Refresh"))), !report && !error ? h("p", { className: "provider-roi-loading", role: "status" }, "Loading Provider ROI…") : null, error ? h(Card, { className: "provider-roi-error", role: "alert" }, h(CardContent, null, error)) : null, report ? h(React.Fragment, null, report.snapshot ? h(Badge, { tone: "secondary" }, "Closed-month snapshot") : null, h(Summary, { summary: report.summary }), report.warnings.length ? h(Card, { className: "provider-roi-warning" }, h(CardContent, null, "Partial data: ", report.warnings.join(" · "))) : null, providers.length ? h(Card, null, h(CardHeader, null, h(CardTitle, null, "Provider comparison")), h(CardContent, null, h("div", { className: "provider-roi-table-head" }, h(SortButton, { label: "Provider", active: sort.key === "provider", desc: sort.desc, onClick: function () { setSort("provider"); } }), h(SortButton, { label: "Activity", active: sort.key === "activity", desc: sort.desc, onClick: function () { setSort("activity"); } }), h("span", null, "Kanban"), h("span", null, "Coverage"), h("span", null, "Verdict"), h("span", null, "Plan")), h("ul", { className: "provider-roi-list" }, list.map(function (provider) { return h(ProviderRow, { key: provider.provider, provider: provider, onRefresh: load }); })))) : h(Card, null, h(CardContent, { className: "provider-roi-empty" }, "No observed or configured providers for this month.")), h(PlanForm, null), h(QuotaForm, null), h(ExceptionForm, null), h("details", { className: "provider-roi-disclosure" }, h("summary", null, "Metric provenance and safeguards"), h("p", null, "Activity is based on locally observed session usage deltas. Kanban totals are global unless a task/provider relation exists. OpenRouter/PAYG is excluded. Nous Portal is critical infrastructure and cannot be cancelled here."), h("p", null, "Private store: ", report.store_path))) : null);
  }
  window.__HERMES_PLUGINS__.register("provider-roi", ProviderRoiPage);
})();
